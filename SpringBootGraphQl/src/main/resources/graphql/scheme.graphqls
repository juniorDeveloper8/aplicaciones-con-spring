# Query solo para hacer consultas
type Query{
    findStudentById(studentId: String) : Student
    findAllStudents : [Student]
}

type Student {
    id: ID,
    name: String,
    lastName: String,
    age: Int
}